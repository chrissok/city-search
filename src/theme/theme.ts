	import { createTheme } from "@mui/material";

	export const theme = createTheme({
		palette: {
			primary: {
				main: "#7e3fb5",
			},
			secondary: {
				main: "rgb(220, 0, 78)",
			},
			background: {
				default: "#fff",
				paper: "#fff",
			},
		},
	});
